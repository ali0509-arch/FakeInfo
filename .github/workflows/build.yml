name: Build

on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore FakeInfo/FakeInfo.csproj

      - name: Build
        run: dotnet build FakeInfo/FakeInfo.csproj --no-restore

      - name: Test
        run: dotnet test FakeInfo/FakeInfo.csproj --no-build
        # test